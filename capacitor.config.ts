import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular_ADS_ProjetoGestao',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
